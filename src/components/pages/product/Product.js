import React from "react";
import AdminLayout from "../../layout/AdminLayout";

export const Product = () => {
  return (
    <div>
      <AdminLayout title={Product}> Product</AdminLayout>
    </div>
  );
};

export default Product;
