import React from "react";
import AdminLayout from "../../layout/AdminLayout";

export const Order = () => {
  return (
    <div>
      <AdminLayout title={Order}> Order</AdminLayout>
    </div>
  );
};

export default Order;
