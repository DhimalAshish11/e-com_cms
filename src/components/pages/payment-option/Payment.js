import React from "react";
import AdminLayout from "../../layout/AdminLayout";

export const Payment = () => {
  return (
    <div>
      <AdminLayout title={Payment}> Payment</AdminLayout>
    </div>
  );
};

export default Payment;
